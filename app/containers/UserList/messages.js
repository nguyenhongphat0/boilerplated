/*
 * UserList Messages
 *
 * This contains all the text for the UserList component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.UserList.header',
    defaultMessage: 'All {d} users',
  },
});
